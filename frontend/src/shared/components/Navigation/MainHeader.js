import MainNavigation from "./MainNavigation";

// Responsible for the main header
const MainHeader = (props) => {
  return (
    <header>
      <MainNavigation />
    </header>
  );
};

export default MainHeader;
